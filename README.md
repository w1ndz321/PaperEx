# PaperEx — 学术论文知识抽取流水线

PDF → Markdown → LLM 抽取结构化知识（10 类知识条目），支持并发 + 断点续跑。

全程状态由 SQLite 管理：早期版本为 `kg_pipeline.db`（根目录），当前版本为 `db/<batch>.db`，每个批次独立库。

---

## 流程概览

```
pdfs/<batch>/*.pdf
    ↓ kg_convert.py      PDF→MD，去重，过滤残缺文件
markdown/<batch>/*.md
    ↓ kg_preprocess.py   LLM 修正 metadata + 学科分类（1次LLM调用）
kg_output/<batch>/*.json  (只有 metadata)
    ↓ kg_extract.py      LLM 抽取 10 类知识（1次LLM调用）
kg_output/<batch>/*.json  最终产物：metadata + entries
```

---

## 实测小结

### 批次对比

| 项目 | 批次 1 | 批次 2 |
|------|--------|--------|
| 数据来源 | 单个zip | 5 个 zip 混合抽样各20%|
| 论文总数 | 1,000 篇 | 1,000 篇 |
| 线程数 | 10 | 128 |
| 有效文件 | 618 篇（61.8%） | 551 篇（55.1%） |
| 跳过原因 | 327 字符不足 + 55 内容重复 | 406 字符不足 + 43 内容重复 |
| 实际处理 | **209 篇**（PROCESS_LIMIT=200） | **551 篇**（全量） |

### 阶段耗时

| 阶段 | 批次 1（10线程·209篇） | 批次 2（128线程·551篇） |
|------|---------------------|---------------------|
| convert（PDF→MD） | 53 min（1000 篇） | **60 min（1000 篇）** |
| preprocess | **94 秒** | **53 秒** |
| extract | **43.7 min（2,624 秒）** | **10.1 min（606 秒）** |
| 全流程合计 | ~46 min | ~70 min |

### Token 消耗

| 阶段 | 批次 1（209篇） | 批次 2（551篇） |
|------|--------------|--------------|
| preprocess | 182,289 + 19,665 = **201,954**（966/篇） | 649,582 + 68,153 = **717,735**（1,302/篇） |
| extract | 1,184,612 + 847,905 = **2,032,517**（9,724/篇） | 3,669,655 + 2,250,018 = **5,919,673**（10,743/篇） |
| **合计** | **2,234,471**（10,690/篇） | **6,637,408**（12,047/篇） |

### 知识类型分布（批次 2，551 篇）

| 类型 | 数量 | 占比 | 均值/篇 |
|------|------|------|---------|
| concept（概念） | 7,117 | 42.4% | 12.9 |
| relation（关系） | 2,406 | 14.3% | 4.4 |
| method（方法） | 1,581 | 9.4% | 2.9 |
| performance_result（性能结果） | 1,449 | 8.6% | 2.6 |
| experiment（实验） | 1,076 | 6.4% | 2.0 |
| claim（主张） | 1,012 | 6.0% | 1.8 |
| conclusion（结论） | 840 | 5.0% | 1.5 |
| limitation（局限性） | 614 | 3.7% | 1.1 |
| future_work（未来工作） | 399 | 2.4% | 0.7 |
| dataset（数据集） | 282 | 1.7% | 0.5 |
| **合计** | **16,776** | **100%** | **30.4** |

> extract 阶段瓶颈在 API 限流（非硬件）：128 线程下 CPU < 1%，内存增量 < 50MB，API 排队等待约占总耗时 50%。

### 样本特征（批次 2，551 篇）

**学科分布**：覆盖 44 个 level1 学科，TOP5 为物理(18.3%)、材料(8.9%)、电子(7.8%)、临床医学(5.6%)、工程基础(5.4%)，文理医工农均有覆盖。

**来源推断**：~52% 期刊 ISSN 编码文件，~13% Physical Review 系列（美国物理学会顶刊），~21% 期刊缩写（如 aac=Antimicrob Agents Chemother, liv=Liver Int 等），~11% SICI 格式，少量 IEEE/ASME 会议论文。

**年份分布**：1970-2026 年，中位数 2017，均值 2010。2020s 占 34%，1990s 占 18%，整体以近 30 年文献为主。
---

## 硬件消耗 & 成本估算（1,000 万篇）

按批次 2 实测（1000 PDF / 128 线程 / 55.1% 有效率）外推。

### convert（PDF→MD）— CPU 密集，4 线程最优

pymupdf4llm 底层 C 库内部自带多核并行，Python 层线程数超过 4 后内部线程争抢 CPU，吞吐反而下降。

| 线程数 | 吞吐 | CPU使用率 | 单机日处理量 | 1,000 万篇耗时 |
|-------|------|-----------|------------|--------------|
| **4** | **35.1 篇/min** | ~21 核 | **~50,600 篇** | **~198 天** |
| 8 | 32.6 篇/min | ~22 核 | ~47,000 篇 | ~213 天 |
| 16 | 29.3 篇/min | ~22 核 | ~42,200 篇 | ~237 天 |

> 实测环境：Intel Xeon Platinum 8380 × 160 核，1007 GB 内存。测试集 100 篇 PDF。

### extract（LLM 知识抽取）— API 密集，按线程数扩展

瓶颈在 API 限流（非硬件），吞吐随线程数近线性增长。

| 方案 | 总线程 | 有效篇数 | 耗时 | 硬件消耗 |
|------|-------|---------|------|---------|
| 128 线程（1 台） | 128 | 551 万 | **~70 天** | CPU<1%, Mem<50MB |
| 256 线程（1 台） | 256 | 551 万 | **~35 天** | 同上 |
| 512 线程（1 台） | 512 | 551 万 | **~17.5 天** | 同上 |
| 1024 线程（1 台） | 1024 | 551 万 | **~8.8 天** | 同上 |

> preprocess 仅 53 秒即可完成 551 篇，相对可忽略，不单独列。

### 费用估算（DeepSeek-v3.2 定价：1 元/百万输入，2 元/百万输出）

| | Tokens | 费用 |
|--|--------|------|
| 输入（preprocess + extract） | ~432 亿（65%） | **4.32 万元** |
| 输出（preprocess + extract） | ~232 亿（35%） | **4.64 万元** |
| **合计** | **~664 亿** | **~8.96 万元** |

> 仅有效论文产生费用，跳过文件不计。实际受缓存命中率、批量折扣影响。

---

## 配置说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `OPENAI_API_KEY` / `OPENAI_BASE_URL` / `LLM_MODEL` | API 鉴权与模型 | deepseek-v3.2 |
| `WORKERS` | 并发线程数 | 128 |
| `MAX_INPUT_CHARS` | extract 单篇最大输入 | 40,000 |
| `MIN_MD_CHARS` | MD 最小字符数（低于此值跳过） | 10,000 |

---

## 使用方法

```bash
python kg_convert.py --input-dir pdfs/<batch>     # PDF→MD + 去重过滤
python kg_preprocess.py markdown/<batch>           # LLM 修正元数据 + 学科分类
python kg_extract.py markdown/<batch>              # LLM 抽取 10 类知识条目
```

状态查看：`python kg_state.py`；重新处理：加 `--force`；调试：加 `--stream`（单线程）。

---

## 注意事项

1. **必须按序运行**：convert → preprocess → extract，extract 依赖 preprocess 的 metadata
2. **中断可续跑**：直接重跑即可，已完成自动跳过；running 超 10 分钟自动重置为 pending
3. **API 限流是唯一瓶颈**：硬件资源充足（CPU < 1%，内存增量 < 50MB），可安全提高 WORKERS

---

## 本阶段优化记录

- **知识类型扩展（10→12）**：新增 `quantitative_result`（量化实验指标）和 `data_specification`（数据格式规范/质量标准），覆盖 AI4S 数据评估场景
- **5 组并行知识抽取**：将 12 种类型拆为 5 组独立 LLM 调用（concept+relation / dataset+spec / method+experiment / quantitative+perf / G5 结论展望），每篇 5 次调用，组间串行防止 token 截断，组内均衡
- **Evidence 强化**：硬约束每条 evidence 8-10 句完整段落，示例同步扩充，median evidence 长度从 ~400 字符提升至 1000+ 字符
- **字段语义优化**：dataset 增 modality（数据模态），experiment 增 setup（实验条件摘要），performance_result 增 metric+compared_to（对比指标），data_spec 精简为 description
- **去掉数量配额**：不再规定每种类型抽取多少条，改为"原文有的全抽、不编造"，论文自身决定产出
- **DB 依赖管理**：preprocess 须 convert done，extract 须 preprocess done；convert 失败自动 skip 后续阶段
- **增量重提取**：DB 记录 failed_groups，重新跑自动只补失败组，不重跑已成功的
- **JSON 截断修复**：LLM 输出超 token 被截断时自动补全 JSON 结构，恢复已抽取条目
- **参考文献过滤**：多模式匹配截掉 References/Bibliography/尾部引用，减少输入噪声
- **SciDB-10 数据集首批结果**：152 篇论文中 145 篇成功抽取，4 篇 PDF 损坏，7 篇 MD5 重复，产出 7322 条知识，覆盖 9 个子数据集